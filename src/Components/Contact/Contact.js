import React from 'react';

const Contact = ({name, email}) => {
    return (
        <div>
            <p>name: {name}</p>
            <p>email: {email}</p>
        </div>
    );
};

export default Contact;
